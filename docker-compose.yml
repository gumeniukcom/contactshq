services:
  app:
    image: ghcr.io/gumeniukcom/contactshq:latest
    ports:
      - "8080:8080"
    environment:
      CHQ_DATABASE_DRIVER: postgres
      CHQ_DATABASE_DSN: "postgres://chq:chq@postgres:5432/contactshq?sslmode=disable"
      # Required. Generate with: openssl rand -hex 32
      # Compose refuses to start if it is unset (see .env.example).
      CHQ_AUTH_JWT_SECRET: ${CHQ_AUTH_JWT_SECRET:?set CHQ_AUTH_JWT_SECRET in .env — generate with `openssl rand -hex 32`}
    volumes:
      - backups:/app/backups
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: chq
      POSTGRES_PASSWORD: chq
      POSTGRES_DB: contactshq
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chq"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
  backups:
